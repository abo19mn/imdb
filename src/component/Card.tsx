/*function Card({movie}) {
    const IMAGE_BASE_URL = ""
  return (
    <div>
      <Link className ="tex-decoration-none " href = {"/movies/" + movie.id}>
        <div className =" card" style= {{width : '15rem' }}>
          <img src={IMAGE_BASE_URL + movie.poster_path} alt="" />
          <div>
          <h5> {movie.title}</h5>
          <p> {movie.description  }</p>
        </div>
      </Link>
    </div>
  );
}

export default Card
*/
